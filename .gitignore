# results.csv is a plain CLI artifact - regenerate with `python pick_path.py`.
# dashboard.html IS checked in, deliberately: it is a ready-to-open snapshot
# built from the sample data, so a bare clone/download works with zero Python
# needed. Refresh it with `python make_dashboard.py` after any change to
# pick_path.py, pickpath_engine.js or the sample CSVs.
results.csv

__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/
.DS_Store
